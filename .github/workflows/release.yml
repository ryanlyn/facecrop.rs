name: release

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [aarch64-apple-darwin, aarch64-unknown-linux-gnu, x86_64-unknown-linux-gnu]

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        target: ${{ matrix.target }}
        override: true

    - name: Install dependencies
      run: sudo apt-get install -y gcc-aarch64-linux-gnu gcc-x86-64-linux-gnu

    - name: Build Release
      uses: actions-rs/cargo@v1
      with:
        command: build
        args: --release --target ${{ matrix.target }}

    - name: Upload Release
      uses: actions/upload-artifact@v2
      with:
        name: facecrop-${{ matrix.target }}
        path: target/${{ matrix.target }}/release/facecrop